/**
 * Placeholder entry point for the OpenAPI package.
 *
 * @param name - Name to greet. Defaults to `world`.
 * @returns The greeting message.
 */
export function hello(name = 'world'): string {
  return `hello ${name}`
}
