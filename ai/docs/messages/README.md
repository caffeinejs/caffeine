# Error message pages

One markdown file per `code` string (`ERR_HTTP_NOT_FOUND.md`). Agents grep the code from a thrown error, then read this folder.

This slice has a single example. Add files as codes become worth documenting. Do not invent URLs in `Err*` constructors until this set is real.
