{
  "name": "@caffeinejs/view",
  "version": "0.0.0",
  "description": "Caffeine template-based server-side rendering plugin",
  "license": "MIT",
  "private": true,
  "type": "module",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean:dist": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/view": "^12.0.0"
  },
  "peerDependencies": {
    "@caffeinejs/di": "*",
    "@caffeinejs/http": "*",
    "@caffeinejs/std": "*",
    "fastify": "^5.0.0",
    "fastify-plugin": "^6.0.0"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "ejs": "^3.1.10",
    "fastify": "^5.12.1",
    "handlebars": "^4.7.9"
  }
}
