services:
  configserver:
    build: .
    ports:
      - '8888:8888'
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:8888/actuator/health']
      interval: 10s
      timeout: 5s
      retries: 5
