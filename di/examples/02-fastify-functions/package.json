{
  "name": "@caffeinejs/example-fastify-functions",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "npm run build --prefix ../../..",
    "start": "node --import tsx/esm index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@caffeinejs/di": "*",
    "fastify": "^5.12.4"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "tsx": "^4.23.13",
    "vitest": "^4.1.8"
  }
}
